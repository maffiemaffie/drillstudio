const stepSize = 16;
const margin = 3;
const playerSize = 1;
const showGuidesAll = true;
const showGuidesSelected = true;

export const config = {
  stepSize,
  gridSize: 8 * stepSize,
  margin: margin * stepSize,
  playerSize,
  showGuidesAll,
  showGuidesSelected
};